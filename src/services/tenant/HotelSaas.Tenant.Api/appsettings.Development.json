{
  "ConnectionStrings": {
    "//": "Connects as hs_tenant_user, never as postgres - ADR-0003. Password matches HS_SERVICE_PASSWORD in infra/docker/.env. Port 5433 is the container; 5432 on this machine is a locally installed postgres.",
    "TenantDb": "Host=localhost;Port=5433;Database=hs_tenant;Username=hs_tenant_user;Password=local_dev_only_change_me;Include Error Detail=true;Maximum Pool Size=20"
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "//": "To see the SQL, set this to Information. LoggingSetup applies its defaults BEFORE ReadFrom.Configuration, so this override wins.",
      "Override": {
        "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
      }
    }
  }
}
